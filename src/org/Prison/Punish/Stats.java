package org.Prison.Punish;

public class Stats {

/*
	public static void addMute(String p){
		if (Files.getDataFile().contains("Players." + p + ".Mute")){
			int old = Files.getDataFile().getInt("Players." + p  + ".Mute");
			int newi = old + 1;
			Files.getDataFile().set("Players." + p  + ".Mute", newi);
			Files.saveDataFile();
		}else{
			Files.getDataFile().set("Players." + p  + ".Mute", 1);
			Files.saveDataFile();
		}
	}
	public static void addBan(String p){
		if (Files.getDataFile().contains("Players." + p  + ".Ban")){
			int old = Files.getDataFile().getInt("Players." + p + ".Ban");
			int newi = old + 1;
			Files.getDataFile().set("Players." + p  + ".Ban", newi);
			Files.saveDataFile();
		}else{
			Files.getDataFile().set("Players." + p  + ".Ban", 1);
			Files.saveDataFile();
		}
	}
	public static void addTemp(String p){
		if (Files.getDataFile().contains("Players." + p  + ".Tempstat")){
			int old = Files.getDataFile().getInt("Players." + p  + ".Tempstat");
			int newi = old + 1;
			Files.getDataFile().set("Players." + p + ".Tempstat", newi);
			Files.saveDataFile();
		}else{
			Files.getDataFile().set("Players." + p  + ".Tempstat", 1);
			Files.saveDataFile();
		}
	}
	
	public static void addKick(String p){
		if (Files.getDataFile().contains("Players." + p  + ".Kick")){
			int old = Files.getDataFile().getInt("Players." + p  + ".Kick");
			int newi = old + 1;
			Files.getDataFile().set("Players." + p + ".Kick", newi);
			Files.saveDataFile();
		}else{
			Files.getDataFile().set("Players." + p  + ".Kick", 1);
			Files.saveDataFile();
		}
	}
	
	public static int getMutes(String p){
		if (Files.getDataFile().contains("Players." + p+ ".Mute")){
			return Files.getDataFile().getInt("Players." + p + ".Mute");
		}
		return 0;
	}
	public static int getBans(String p){
		if (Files.getDataFile().contains("Players." + p + ".Ban")){
			return Files.getDataFile().getInt("Players." + p + ".Ban");
		}
		return 0;
	}
	public static int getTemps(String p){
		if (Files.getDataFile().contains("Players." + p + ".Tempstat")){
			return Files.getDataFile().getInt("Players." + p + ".Tempstat");
		}
		return 0;
	}
	public static int getKicks(String p){
		if (Files.getDataFile().contains("Players." + p + ".Kick")){
			return Files.getDataFile().getInt("Players." + p + ".Kick");
		}
		return 0;
	}
	*/
}
